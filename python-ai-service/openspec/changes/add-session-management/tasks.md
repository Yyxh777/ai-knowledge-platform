## 1. 数据模型与存储准备

- [ ] 1.1 设计并创建 `chat_sessions`、`chat_messages` 表结构（含主键、外键、时间字段）
- [ ] 1.2 为会话列表与历史查询添加必要索引（如 `user_id+updated_at`、`session_id+created_at`）
- [ ] 1.3 在 `utils/conversation_db.py` 增加会话与消息的基础 CRUD 数据访问方法
- [ ] 1.4 编写幂等初始化/迁移逻辑，确保旧环境可无损升级

## 2. 会话管理 API 实现

- [ ] 2.1 在 `models` 中新增会话相关请求/响应模型（创建、列表、详情、删除）
- [ ] 2.2 在 `service` 层实现会话创建、列表、详情、删除能力，并统一封装 `user_id + session_id` 权限校验
- [ ] 2.3 在 `api` 层新增会话管理路由并接入认证上下文
- [ ] 2.4 为会话列表接口添加分页或数量限制参数，避免全量返回

## 3. 聊天链路兼容扩展

- [ ] 3.1 扩展聊天请求模型支持可选 `session_id`
- [ ] 3.2 在聊天入口实现“有 `session_id` 复用、无 `session_id` 自动创建”的流程
- [ ] 3.3 将每轮用户/助手消息持久化到对应会话历史，并回写 `session_id` 到响应
- [ ] 3.4 更新 `workflows/rag_graph.py` 与相关服务调用，确保会话上下文参数传递一致

## 4. 查询、校验与测试

- [ ] 4.1 实现会话历史消息查询接口，按 `created_at` 升序分页返回
- [ ] 4.2 为跨用户访问场景补充权限拒绝逻辑与统一错误响应
- [ ] 4.3 编写单元测试覆盖会话 CRUD、自动建会话、历史写入与分页查询
- [ ] 4.4 编写接口集成测试覆盖“访问他人会话被拒绝”等关键场景

## 5. 联调与发布准备

- [ ] 5.1 联调前端会话列表与历史回放，验证兼容旧聊天调用
- [ ] 5.2 补充配置说明与接口文档（包含 `session_id` 字段语义）
- [ ] 5.3 进行回滚演练：停用新会话入口后旧聊天流程仍可正常运行
